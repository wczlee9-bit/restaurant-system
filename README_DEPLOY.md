# 🎉 多店铺扫码点餐系统 - 部署就绪

## 📦 项目已打包完成

您的系统代码已打包完成，准备部署到服务器！

---

## 📍 打包文件位置

```
/tmp/restaurant-system.tar.gz (402KB)
```

包含以下内容：
- ✅ `src/` - 完整的源代码
- ✅ `requirements.txt` - Python 依赖列表
- ✅ `scripts/` - 部署和工具脚本
- ✅ `docs/` - 完整文档
- ✅ `.coze` - 项目配置

---

## 🚀 开始部署

### 方法 1：使用一键部署脚本（推荐）

#### 步骤 1：下载打包文件

**如果您在这个沙盒环境中**，文件已在 `/tmp/restaurant-system.tar.gz`

**如果您需要从服务器下载**：
```bash
# 在服务器上执行
cd /opt
wget http://your-server-ip/path/to/restaurant-system.tar.gz
# 或使用 scp 从本地上传
```

#### 步骤 2：上传到服务器

**在您的本地电脑**（新开终端）：
```bash
# 如果您能访问沙盒环境的文件系统
# 使用 scp 上传到服务器
scp /tmp/restaurant-system.tar.gz root@your-server-ip:/opt/
```

#### 步骤 3：解压并部署

**在服务器上**（SSH 连接后执行）：
```bash
# 进入目标目录
cd /opt

# 解压项目
tar -xzf restaurant-system.tar.gz
cd restaurant-system

# 赋予执行权限
chmod +x scripts/deploy_to_server.sh

# 复制到根目录便于执行
cp scripts/deploy_to_server.sh .
chmod +x deploy_to_server.sh

# 运行一键部署脚本
bash deploy_to_server.sh
```

**部署脚本会自动完成**：
- ✅ 更新系统
- ✅ 安装 Python、PostgreSQL、Nginx
- ✅ 配置数据库
- ✅ 初始化数据
- ✅ 启动所有服务

**等待 5-10 分钟即可完成！**

---

### 方法 2：手动部署

如果您需要更精细的控制，请参考：
- 📖 **详细部署指南**：`docs/服务器部署指南.md`
- ⚡ **快速参考**：`docs/快速部署参考.md`

---

## 🧪 部署后测试

部署完成后，使用以下命令验证：

```bash
# 1. 检查服务状态
systemctl status restaurant

# 2. 测试健康检查
curl http://127.0.0.1:8080/health

# 3. 测试 API
curl http://127.0.0.1:8080/api/store

# 4. 查看日志
journalctl -u restaurant -f
```

**预期结果**：
```json
{"status":"healthy","service":"restaurant-system"}
```

---

## 📚 完整测试

部署成功后，使用测试手册进行完整的功能测试：

- 📖 **测试手册**：`docs/测试手册.md`

**包含 6 个完整业务流程**：
1. ✅ 客户点餐流程
2. ✅ 厨师做菜流程
3. ✅ 传菜员流程
4. ✅ 收款员流程
5. ✅ 店铺设置流程
6. ✅ 财务统计流程

---

## 🔧 默认配置

| 配置项 | 默认值 |
|--------|--------|
| **后端端口** | 8080 |
| **Nginx 端口** | 80 |
| **数据库名** | restaurant_db |
| **数据库用户** | restaurant_user |
| **数据库密码** | Restaurant@2024 |

**自定义配置**：
```bash
export DB_PASSWORD="YourSecurePassword"
export DB_NAME="my_database"
export DB_USER="my_user"
export DOMAIN="your-domain.com"
bash deploy_to_server.sh
```

---

## 📖 文档列表

| 文档 | 说明 |
|------|------|
| `docs/服务器部署指南.md` | 详细的部署步骤和说明 |
| `docs/快速部署参考.md` | 快速命令参考卡片 |
| `docs/测试手册.md` | 完整的 API 测试指南 |
| `scripts/deploy_to_server.sh` | 一键部署脚本 |

---

## 🛠️ 常用管理命令

```bash
# 服务管理
systemctl start restaurant    # 启动
systemctl stop restaurant     # 停止
systemctl restart restaurant  # 重启
systemctl status restaurant   # 状态

# 日志查看
journalctl -u restaurant -f   # 实时日志

# 数据库操作
sudo -u postgres psql -d restaurant_db  # 连接数据库
```

---

## ⚠️ 重要提示

1. **服务器要求**：
   - Ubuntu 20.04/22.04 LTS（推荐）
   - 最低配置：2核CPU、4GB内存

2. **防火墙配置**：
   - 确保开放端口：22 (SSH)、80 (HTTP)、443 (HTTPS)
   - 腾讯云安全组需要放行这些端口

3. **首次部署**：
   - 脚本会自动安装所有依赖
   - 数据库密码默认为 `Restaurant@2024`
   - 建议部署后修改默认密码

4. **生产环境**：
   - 配置 HTTPS（使用 Let's Encrypt）
   - 定期备份数据库
   - 监控服务状态

---

## ❓ 遇到问题？

### 快速诊断

```bash
# 检查服务状态
systemctl status restaurant

# 检查端口占用
lsof -i :8080

# 查看错误日志
journalctl -u restaurant -n 50

# 测试数据库连接
sudo -u postgres psql -U restaurant_user -d restaurant_db -c "SELECT 1"
```

### 获取帮助

提供以下信息以便快速定位问题：
1. 服务器操作系统版本
2. 错误信息截图
3. 服务日志（`journalctl -u restaurant -n 100`）
4. 执行的具体命令

---

## 🎯 下一步

部署成功后，您可以：

1. ✅ **执行功能测试** - 使用测试手册验证所有功能
2. ✅ **配置域名和 HTTPS** - 申请证书并配置 SSL
3. ✅ **对接前端** - 将 Vue.js 前端部署到服务器
4. ✅ **配置监控** - 设置日志收集和告警

---

**祝您部署顺利！** 🎉

如有任何问题，请参考文档或提供详细信息以便排查。

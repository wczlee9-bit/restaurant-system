# 🌐 通过浏览器直接访问测试系统

## 📯 适用情况

如果您：
- ✅ 看到的是网页预览界面
- ✅ 看不到左侧文件树
- ✅ 无法直接打开本地文件
- ✅ 只能通过URL访问

---

## 🚀 直接访问方式

### 方式1：主入口页面（推荐）

**URL**：
```
http://9.128.251.82:8080/assets/index.html
```

**操作**：
1. 复制上面的URL
2. 在浏览器地址栏中粘贴
3. 按回车键访问
4. 点击 "⚡ 立即测试8号桌完整流程" 按钮
5. 开始测试！

---

### 方式2：直接测试页面

**URL**：
```
http://9.128.251.82:8080/assets/restaurant_full_test.html?table=8
```

**操作**：
1. 复制上面的URL
2. 在浏览器地址栏中粘贴
3. 按回车键访问
4. 页面自动选择8号桌
5. 开始点餐测试

---

### 方式3：API文档

**URL**：
```
http://9.128.251.82:8000/docs
```

**用途**：查看API接口文档

---

## 📋 完整URL列表

| 用途 | URL |
|------|-----|
| **主入口** | http://9.128.251.82:8080/assets/index.html |
| **测试页面** | http://9.128.251.82:8080/assets/restaurant_full_test.html |
| **8号桌测试** | http://9.128.251.82:8080/assets/restaurant_full_test.html?table=8 |
| **API文档** | http://9.128.251.82:8000/docs |
| **健康检查** | http://9.128.251.82:8000/health |

---

## 🎯 一键复制测试URL

### 测试8号桌完整流程
```
http://9.128.251.82:8080/assets/restaurant_full_test.html?table=8
```

**步骤**：
1. 复制上面的URL
2. 在浏览器中打开
3. 开始测试！

---

## 📱 测试流程

### 通过URL访问后：

```
1. 打开: http://9.128.251.82:8080/assets/restaurant_full_test.html?table=8
2. 页面自动选择8号桌
3. 浏览菜单，添加菜品
4. 提交订单
5. 切换角色：👤顾客 → 👨‍🍳厨师 → 🤵传菜员 → 💰收银员 → 👔店长
```

---

## 💡 如果URL无法访问

### 原因1：IP地址变更

**检查服务状态**：
```bash
# 在终端中运行
netstat -tlnp | grep -E ":(8000|8080)"
```

**查看新的IP**：
```bash
ip addr show | grep "inet "
```

### 原因2：服务未启动

**启动服务**：
```bash
# 启动API服务（端口8000）
cd /workspace/projects
python -m uvicorn src.api.restaurant_api:app --host 0.0.0.0 --port 8000 &

# 启动HTTP服务（端口8080）
python -m http.server 8080 --bind 0.0.0.0 &
```

### 原因3：防火墙阻止

**检查防火墙**：
```bash
# 临时关闭防火墙（测试用）
sudo systemctl stop firewalld
# 或
sudo ufw disable
```

---

## 🔧 更新URL（如果IP变更）

### 查看当前服务端口

```bash
netstat -tlnp | grep -E ":(8000|8080)"
```

### 查看当前IP地址

```bash
ip addr show | grep "inet " | grep -v "127.0.0.1"
```

### 构建新的URL

格式：
```
http://[您的IP地址]:8080/assets/restaurant_full_test.html?table=8
```

示例：
```
http://192.168.1.100:8080/assets/restaurant_full_test.html?table=8
```

---

## 🎮 快速开始（3步）

### 第1步：复制URL

```
http://9.128.251.82:8080/assets/restaurant_full_test.html?table=8
```

### 第2步：在浏览器中打开

在浏览器地址栏中粘贴并访问

### 第3步：开始测试

按照页面指引完成测试

---

## 📊 服务状态检查

### 在浏览器中访问以下URL检查服务：

| 检查项 | URL | 预期结果 |
|--------|-----|----------|
| HTTP服务 | http://9.128.251.82:8080/ | 显示文件列表 |
| 测试页面 | http://9.128.251.82:8080/assets/index.html | 显示主入口 |
| API服务 | http://9.128.251.82:8000/health | 显示"API服务正常" |
| API文档 | http://9.128.251.82:8000/docs | 显示API文档 |

---

## ❓ 常见问题

### Q1: URL打不开？

**A**：
1. 检查网络连接
2. 确认服务是否运行
3. 尝试使用localhost

```
http://localhost:8080/assets/restaurant_full_test.html?table=8
```

### Q2: 显示"拒绝连接"？

**A**：
1. 检查端口是否正确
2. 确认服务是否启动
3. 检查防火墙设置

### Q3: 页面打开但数据为空？

**A**：
1. 等待3秒让数据加载
2. 检查浏览器控制台是否有错误
3. 确认API服务是否运行

---

## 🎉 立即测试

**复制以下URL并打开**：

```
http://9.128.251.82:8080/assets/restaurant_full_test.html?table=8
```

---

**💡 提示**：如果上面的URL无法访问，请检查服务状态或联系管理员获取正确的IP地址。

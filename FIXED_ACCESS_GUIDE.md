# 🍽️ 餐饮点餐系统 - 测试访问方案

## ⚠️ 502错误解决方案

如果您看到"HTTP ERROR 502"，这是因为外部IP访问被阻止了。

---

## ✅ 使用localhost访问（推荐）

### 方式1：测试8号桌（最简单）

**URL**：
```
http://localhost:8080/assets/restaurant_full_test.html?table=8
```

**操作**：
1. 复制上面的URL
2. 在浏览器地址栏中粘贴
3. 按回车访问
4. 开始测试！

---

### 方式2：主入口页面

**URL**：
```
http://localhost:8080/assets/index.html
```

---

### 方式3：API文档

**URL**：
```
http://localhost:8000/docs
```

---

## 📋 完整URL列表（localhost版本）

| 用途 | URL |
|------|-----|
| **8号桌测试** | http://localhost:8080/assets/restaurant_full_test.html?table=8 |
| **主入口** | http://localhost:8080/assets/index.html |
| **测试页面** | http://localhost:8080/assets/restaurant_full_test.html |
| **API文档** | http://localhost:8000/docs |
| **健康检查** | http://localhost:8000/health |

---

## 🎯 立即测试

**复制这个URL到浏览器**：

```
http://localhost:8080/assets/restaurant_full_test.html?table=8
```

---

## 💡 为什么用localhost？

**原因**：
- 外部IP（9.128.251.82）访问被防火墙阻止
- localhost 访问无需通过网络，直接连接本地服务
- 速度更快，更稳定

**适用场景**：
- ✅ 您在沙盒环境中
- ✅ 浏览器和服务在同一台机器上
- ✅ 外部IP无法访问

---

## 🎮 测试流程

打开页面后：

```
👤 顾客 → 点餐（添加菜品→提交订单）
👨‍🍳 厨师 → 制作（开始制作→完成制作）
🤵 传菜员 → 上菜（确认上菜）
💰 收银员 → 结账（处理支付）
👔 店长 → 查看数据
```

---

## ❓ 如果localhost也无法访问

### 方案1：检查服务是否运行

在终端中运行：
```bash
netstat -tlnp | grep -E ":(8000|8080)"
```

应该看到：
```
tcp  0  0  0.0.0.0:8000  LISTEN  ...
tcp  0  0  0.0.0.0:8080  LISTEN  ...
```

### 方案2：重启服务

```bash
# 杀掉旧进程
pkill -f "python.*http.server 8080"
pkill -f "uvicorn.*restaurant_api:app"

# 重启服务
cd /workspace/projects
python -m http.server 8080 --bind 0.0.0.0 &
python -m uvicorn src.api.restaurant_api:app --host 0.0.0.0 --port 8000 &
```

### 方案3：使用127.0.0.1

如果localhost不行，尝试使用127.0.0.1：
```
http://127.0.0.1:8080/assets/restaurant_full_test.html?table=8
```

---

## 🔧 如果需要外部访问

如果您确实需要从外部机器访问，需要：

1. **检查防火墙**
```bash
# 查看防火墙状态
sudo firewall-cmd --state
# 或
sudo ufw status
```

2. **开放端口**
```bash
# 使用firewalld
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload

# 或使用ufw
sudo ufw allow 8080/tcp
sudo ufw allow 8000/tcp
```

3. **检查网络配置**
```bash
# 检查是否监听在0.0.0.0
netstat -tlnp | grep -E ":(8000|8080)"
```

应该显示 `0.0.0.0:8000` 而不是 `127.0.0.1:8000`

---

## 🎉 现在开始测试

**复制这个URL到浏览器**：

```
http://localhost:8080/assets/restaurant_full_test.html?table=8
```

**就这么简单！开始测试吧！** 🎮

---

## 📞 需要帮助？

如果localhost也无法访问，请提供以下信息：

1. 浏览器控制台错误（F12 → Console）
2. 网络标签中的请求详情（F12 → Network）
3. 运行 `netstat -tlnp | grep -E ":(8000|8080)"` 的输出

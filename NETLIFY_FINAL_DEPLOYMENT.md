# 🚀 Netlify 最终部署指南（推荐）

既然Netlify可以访问了，这是最简单可靠的部署方案！

## 📋 部署概览

- **托管平台**：Netlify
- **GitHub仓库**：wczlee9-bit/restaurant-system
- **前端目录**：assets
- **部署方式**：自动从GitHub部署
- **预计时间**：5-10分钟

---

## 🎯 完整部署步骤

### 第1步：访问Netlify

在浏览器中打开：

```
https://app.netlify.com
```

---

### 第2步：登录Netlify

1. 点击右上角的 **"Sign up"** 或 **"Log in"**
2. 选择 **"Sign in with GitHub"**
3. 授权Netlify访问你的GitHub账户
4. 确认授权范围

---

### 第3步：创建新站点

登录后：

1. 点击左侧菜单的 **"Sites"** （站点）
2. 点击右上角的 **"Add new site"** 按钮
3. 选择 **"Import an existing project"** （导入现有项目）

---

### 第4步：连接GitHub仓库

1. 在 **"Continuous Deployment"** 页面：
   - 点击 **"GitHub"** 图标
   - 如果提示授权，点击 **"Authorize Netlify"**
   - 等待仓库列表加载

2. 选择仓库：
   - 在仓库列表中找到 `wczlee9-bit/restaurant-system`
   - 点击仓库右侧的 **"Import site"** （导入站点）

---

### 第5步：配置构建设置

在 **"Import site"** 页面，配置以下内容：

#### 基本设置

| 配置项 | 设置值 | 说明 |
|-------|--------|------|
| **Team** | （选择你的团队） | 默认即可 |
| **Site name** | `restaurant-system` 或自定义 | 站点名称，会出现在URL中 |

#### 构建设置 ⚠️ 重要！

| 配置项 | 设置值 | 说明 |
|-------|--------|------|
| **Build command** | 留空（不填） | 静态站点无需构建 |
| **Publish directory** | `assets` | **必须设置为 assets** |
| **Branch to deploy** | `main` | 部署的分支 |

#### 高级设置（可选）

点击 **"Advanced"** 展开：

- **Node version**: `18` （可以不设置，会自动检测）
- **Functions directory**: 留空

---

### 第6步：部署站点

1. 确认所有设置正确
2. 点击底部的 **"Deploy site"** （部署站点）按钮
3. 等待部署完成（通常1-3分钟）
4. 看到绿色 ✅ 和 **"Published"** 表示部署成功

---

### 第7步：访问你的网站

部署成功后：

1. Netlify会生成一个随机URL，例如：
   ```
   https://amazing-pudding-123456.netlify.app
   ```

2. 点击站点名称或URL访问

3. 主页会自动重定向到门户页面，或直接访问：
   ```
   https://your-site-name.netlify.app/portal.html
   ```

---

### 第8步：配置站点名称（可选）

如果想使用更友好的URL：

1. 进入站点的 **"Site settings"**
2. 点击 **"Change site name"**
3. 输入新的站点名称，例如：`restaurant-system`
4. 保存后，新的URL为：
   ```
   https://restaurant-system.netlify.app
   ```

---

## 🌐 所有功能页面访问地址

部署成功后，可以访问以下页面（替换 `your-site-name` 为你的站点名称）：

| 功能 | URL |
|-----|-----|
| 🏠 门户首页 | `https://your-site-name.netlify.app/portal.html` |
| 👤 顾客点餐 | `https://your-site-name.netlify.app/customer_order_v3.html` |
| 🏪 工作人员登录 | `https://your-site-name.netlify.app/login_standalone.html` |
| 👨‍🍳 厨师工作台 | `https://your-site-name.netlify.app/kitchen_display.html` |
| 🍽️ 订单管理 | `https://your-site-name.netlify.app/staff_workflow.html` |
| 📋 菜品管理 | `https://your-site-name.netlify.app/menu_management.html` |
| 📦 库存管理 | `https://your-site-name.netlify.app/inventory_management.html` |
| 🏬 店铺设置 | `https://your-site-name.netlify.app/shop_settings.html` |
| 👥 会员中心 | `https://your-site-name.netlify.app/member_center.html` |
| 🏢 总公司后台 | `https://your-site-name.netlify.app/headquarters_dashboard.html` |
| 💰 结算管理 | `https://your-site-name.netlify.app/settlement_management.html` |
| 🎁 优惠管理 | `https://your-site-name.netlify.app/discount_management.html` |

**便捷短链接**（通过netlify.toml配置）：

- 主页：`https://your-site-name.netlify.app/`
- 登录：`https://your-site-name.netlify.app/login`
- 点餐：`https://your-site-name.netlify.app/customer-order`
- 会员：`https://your-site-name.netlify.app/member-center`
- 总部：`https://your-site-name.netlify.app/headquarters`

---

## ⚠️ 重要：API配置说明

### Netlify的API代理配置

你的项目已经配置了 `netlify.toml` 文件，包含API代理规则：

- 所有 `/api/*` 请求会自动代理到后端服务器 `115.191.1.219`
- WebSocket连接也自动代理到后端服务器
- 后端服务器必须对外开放以下端口：
  - 8000: 餐饮系统主API
  - 8001: 订单和WebSocket API
  - 8004: 会员API
  - 8006: 总公司API
  - 8007: 增强功能API

### 检查后端服务状态

确保后端API服务器正常运行：

```bash
# 在本地执行，测试API连接
curl http://115.191.1.219:8000/api/health
curl http://115.191.1.219:8001/api/health
curl http://115.191.1.219:8004/api/health
curl http://115.191.1.219:8006/api/health
curl http://115.191.1.219:8007/api/health
```

**如果请求失败**：
1. 检查后端服务是否启动
2. 检查防火墙是否开放这些端口
3. 确认后端配置了CORS允许Netlify域名

---

## ✅ 部署验证清单

部署完成后，按以下顺序验证：

### 1️⃣ 基础访问验证

- [ ] 访问主页，页面正常加载
- [ ] 页面样式正确显示
- [ ] 所有链接可以点击
- [ ] 无404错误

### 2️⃣ 顾客端功能测试

- [ ] 进入顾客点餐页面
- [ ] 菜单列表加载成功
- [ ] 可以添加商品到购物车
- [ ] 可以提交订单
- [ ] 可以完成支付流程

### 3️⃣ 工作人员端测试

- [ ] 进入登录页面
- [ ] 使用演示账号登录成功
- [ ] 可以查看订单列表
- [ ] 可以更新订单状态
- [ ] WebSocket实时推送正常

### 4️⃣ 管理功能测试

- [ ] 菜品管理功能正常
- [ ] 库存管理功能正常
- [ ] 店铺设置功能正常
- [ ] 会员中心功能正常
- [ ] 总公司数据统计正常
- [ ] 结算管理功能正常
- [ ] 优惠管理功能正常

### 5️⃣ API连接测试

打开浏览器开发者工具（F12）→ Network标签：

- [ ] API请求状态为200
- [ ] 无CORS错误
- [ ] WebSocket连接成功

### 6️⃣ 移动端测试

- [ ] 手机访问页面正常显示
- [ ] 触摸操作流畅
- [ ] 可以正常点餐下单
- [ ] 支付功能可用

---

## 🔧 Netlify配置说明

### netlify.toml 文件

项目根目录的 `netlify.toml` 文件包含：

- **构建设置**：指定发布目录为 `assets`
- **路由重定向**：短链接规则（如 `/login` → `/login_standalone.html`）
- **API代理**：将所有API请求代理到后端服务器
- **缓存策略**：优化静态资源加载
- **安全头部**：增强安全性

### API代理配置

```toml
# API代理到后端服务器
[[redirects]]
  from = "/api/*"
  to = "http://115.191.1.219:8000/api/:splat"
  status = 200
  force = true
```

这意味着前端请求 `https://your-site.netlify.app/api/orders` 会自动转发到 `http://115.191.1.219:8000/api/orders`

---

## 🔄 自动部署

Netlify会自动检测GitHub仓库的变化：

- 每次推送到 `main` 分支，会自动触发部署
- 可以在Netlify的 **"Deploys"** 页面查看部署历史
- 支持手动触发重新部署

### 手动触发部署

如果需要手动重新部署：

1. 进入站点的 **"Deploys"** 页面
2. 点击右上角的 **"Trigger deploy"**
3. 选择 **"Deploy site"** 或 **"Deploy hook"**

---

## 🎨 自定义域名（可选）

如果想使用自己的域名：

### 方法1：Netlify DNS

1. 进入 **"Site settings"** → **"Domain management"**
2. 点击 **"Add custom domain"**
3. 输入域名，例如：`restaurant.yourdomain.com`
4. Netlify会提供DNS配置说明
5. 在域名服务商处添加DNS记录

### 方法2：外部DNS

1. 在域名服务商处添加CNAME记录：
   ```
   主机记录: restaurant
   记录类型: CNAME
   记录值: your-site-name.netlify.app
   ```
2. 在Netlify中添加自定义域名
3. 验证域名所有权

---

## 🛠️ 常见问题

### Q1: 部署失败，显示 "Build failed"

**A**: 检查以下几点：
1. 确认 **Publish directory** 设置为 `assets`（不是 `/assets`）
2. 确认assets目录下有 `portal.html` 文件
3. 查看部署日志，找到具体错误

---

### Q2: 页面显示 404 Not Found

**A**:
1. 检查URL是否正确
2. 确认文件名拼写正确
3. 查看部署日志，确认文件是否成功上传

---

### Q3: API请求失败，显示 CORS 错误

**A**:
1. 检查后端API服务是否运行
2. 确认后端配置了CORS允许Netlify域名
3. 在后端API中添加以下CORS配置：

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-site-name.netlify.app",
        "http://localhost:8080"
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

### Q4: WebSocket连接失败

**A**:
1. 确认后端WebSocket服务运行在正确端口
2. 检查netlify.toml中的WebSocket代理配置
3. 确保后端配置了wss://协议（用于HTTPS）

---

### Q5: 页面样式异常

**A**:
1. 清除浏览器缓存（Ctrl + Shift + Delete）
2. 按 Ctrl + F5 强制刷新
3. 检查CDN资源是否加载

---

### Q6: 部署后看不到最新更改

**A**:
1. 等待几分钟，让Netlify缓存刷新
2. 清除浏览器缓存
3. 在Netlify中触发手动部署

---

## 📊 监控和日志

### 查看部署日志

1. 进入站点的 **"Deploys"** 页面
2. 点击任意一次部署
3. 查看 **"Deploy log"** 中的详细信息

### 查看函数日志

如果使用了Netlify Functions：
1. 进入 **"Functions"** 标签
2. 查看函数执行日志

### 查看访问统计

1. 进入 **"Site analytics"**
2. 查看访问量、页面浏览量等数据

---

## 🆘 获取帮助

如果遇到问题：

1. **查看Netlify文档**：https://docs.netlify.com
2. **查看项目文档**：
   - `NETLIFY_DEPLOYMENT.md`
   - `QUICK_START.md`
3. **查看部署日志**：在Netlify站点中查看详细错误信息
4. **提供错误信息**：告诉我具体的错误信息和截图

---

## ✅ 部署完成检查清单

- [ ] Netlify账号登录成功
- [ ] GitHub仓库成功连接
- [ ] 构建设置正确（Publish directory = assets）
- [ ] 首次部署成功
- [ ] 可以访问主页
- [ ] 所有功能页面可访问
- [ ] API连接正常
- [ ] 顾客点餐功能测试通过
- [ ] 工作人员登录测试通过
- [ ] 会员中心功能测试通过
- [ ] 总公司后台功能测试通过
- [ ] 移动端访问测试通过
- [ ] （可选）自定义域名配置完成

---

**开始部署吧！🚀**

访问：https://app.netlify.com

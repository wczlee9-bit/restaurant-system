# 🚀 快速部署参考卡片

## 方式一：我有项目代码（推荐）

### 1️⃣ 连接服务器
```bash
ssh root@your-server-ip
```

### 2️⃣ 上传代码（在本地电脑执行）
```bash
# 进入项目目录
cd /path/to/your/project

# 压缩项目
tar -czf restaurant-system.tar.gz src/ requirements.txt scripts/ docs/ .coze

# 上传到服务器
scp restaurant-system.tar.gz root@your-server-ip:/opt/
```

### 3️⃣ 在服务器上部署
```bash
# SSH 连接后执行
cd /opt
tar -xzf restaurant-system.tar.gz
cd restaurant-system

# 赋予执行权限
chmod +x deploy_to_server.sh

# 运行一键部署脚本
bash deploy_to_server.sh
```

**等待 5-10 分钟，完成！** ✅

---

## 方式二：我需要获取代码

### 在沙盒环境中打包代码

由于我无法直接访问您的服务器，您需要在沙盒环境中先打包代码：

```bash
# 在沙盒环境执行
cd /workspace/projects
tar -czf restaurant-system.tar.gz \
    src/ \
    requirements.txt \
    scripts/ \
    docs/ \
    .coze

# 下载到本地（如果支持文件下载）
# 或使用 scp/curl 等方式获取
```

### 然后按照方式一的上传步骤操作

---

## 验证部署

```bash
# 测试服务
curl http://127.0.0.1:8080/health

# 测试 API
curl http://127.0.0.1:8080/api/store

# 查看服务状态
systemctl status restaurant

# 查看日志
journalctl -u restaurant -f
```

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `systemctl start restaurant` | 启动服务 |
| `systemctl stop restaurant` | 停止服务 |
| `systemctl restart restaurant` | 重启服务 |
| `journalctl -u restaurant -f` | 查看实时日志 |
| `curl http://127.0.0.1:8080/api/store` | 测试 API |

---

## 默认配置

| 项目 | 默认值 |
|------|--------|
| 后端端口 | 8080 |
| Nginx 端口 | 80 |
| 数据库名 | restaurant_db |
| 数据库用户 | restaurant_user |
| 数据库密码 | Restaurant@2024 |

---

## 需要帮助？

- 📖 详细文档：`docs/服务器部署指南.md`
- 🧪 测试手册：`docs/测试手册.md`
- ❓ 常见问题：查看部署指南中的"常见问题"章节

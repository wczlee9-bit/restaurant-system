# GitHub Pages 启用详细步骤指南

## 找到 Settings 的方法

### 方法 1：从当前页面操作

1. **点击仓库顶部的仓库名称**
   - 在你提供的页面顶部，点击：
     ```
     wczlee9-bit / restaurant-system
     ```
   - 这会带你去到仓库主页面

2. **在仓库主页面查找 Settings**
   - 在主页面顶部导航栏，你会看到：
     ```
     ⭐ Star  0  🍴 Fork 0  📂 Watch 0
     ```
   - 在这些按钮的**右侧**（或者在最右侧），应该有一个 **⚙️ Settings** 按钮
   - 点击它

3. **进入 Pages 设置**
   - 在 Settings 页面左侧菜单
   - 滚动到最下方
   - 找到 **Pages** 选项
   - 点击它

### 方法 2：直接访问 URL

直接访问以下 URL（替换你的仓库名）：

```
https://github.com/wczlee9-bit/restaurant-system/settings/pages
```

### 方法 3：通过浏览器地址栏

1. 看当前 URL，应该是：
   ```
   https://github.com/wczlee9-bit/restaurant-system
   ```
2. 在 URL 后面添加 `/settings/pages`
3. 按回车访问

## 配置 GitHub Pages

找到 Pages 设置后，按以下步骤操作：

### 步骤 1：选择部署方式

在 **Build and deployment** 部分：

```
Source: [GitHub Actions ▼]  <-- 选择这个！
```

**不要选择** `Deploy from a branch`，一定要选择 `GitHub Actions`。

### 步骤 2：保存配置

点击 **Save** 按钮。

### 步骤 3：等待自动部署

配置保存后：

1. 点击仓库顶部的 **Actions** 标签
2. 你会看到一个工作流正在运行（或已运行完成）
3. 工作流名称是：**deploy**（或 **Deploy to GitHub Pages**）
4. 等待 2-3 分钟，状态应该变成 ✅ 绿色勾号

### 步骤 4：获取部署地址

部署完成后：

1. 回到 **Settings → Pages**
2. 在页面顶部，你会看到：
   ```
   🌐 Deployed GitHub Pages site

   Your site is live at:
   https://wczlee9-bit.github.io/restaurant-system/
   ```
3. 点击这个链接访问你的前端站点

## 验证部署

### 检查 Actions 状态

1. 点击 **Actions** 标签
2. 点击 **deploy** 工作流
3. 查看最新的运行记录
4. 状态应该是 ✅ Success（绿色勾号）
5. 点击运行记录可以查看详细日志

### 访问前端页面

部署成功后，访问以下页面：

- **主入口**：`https://wczlee9-bit.github.io/restaurant-system/`
- **点餐页面**：`https://wczlee9-bit.github.io/restaurant-system/customer_order_v3.html`
- **门户页面**：`https://wczlee9-bit.github.io/restaurant-system/portal.html`

### 测试 API 连接

1. 打开任意前端页面
2. 按 F12 打开浏览器开发者工具
3. 切换到 **Console**（控制台）标签
4. 应该能看到正常的 API 请求，没有错误

## 常见问题

### Q: 找不到 Settings 按钮？

**可能的原因**：
1. 权限不足（只有 Read 权限，没有 Admin 权限）
2. 使用了错误的 GitHub 账号登录

**解决方法**：
1. 确认你是仓库所有者（Owner）
2. 检查 Collaborators 设置：Settings → Collaborators

### Q: Pages 选项是灰色的/不可用？

**可能的原因**：
1. 仓库是 Fork 的仓库
2. 私人仓库需要启用 GitHub Pages 功能

**解决方法**：
1. 如果是 Fork 的仓库，可能需要创建新仓库
2. 检查仓库类型：Settings → General → Danger Zone

### Q: 部署失败？

**查看错误日志**：
1. 进入 Actions 标签
2. 点击失败的运行记录
3. 查看错误信息

**常见错误**：
- `Error: The assets directory is empty`：检查 `assets` 目录是否存在且有文件
- `Error: Failed to upload artifact`：网络问题，重新运行工作流

### Q: 访问页面 404？

**可能的原因**：
1. 部署还在进行中（等待 1-2 分钟）
2. 访问了错误的 URL
3. 浏览器缓存

**解决方法**：
1. 等待部署完成
2. 确认 URL 格式正确
3. 清除浏览器缓存或使用隐私模式

### Q: API 调用失败？

**可能的原因**：
1. `_redirects` 文件没有正确部署
2. 后端服务未启动

**解决方法**：
1. 检查 `assets/_redirects` 是否存在
2. 测试后端健康检查：`https://restaurant-system-vzj0.onrender.com/health`

## 快速导航链接

- **仓库主页**：https://github.com/wczlee9-bit/restaurant-system
- **Settings**：https://github.com/wczlee9-bit/restaurant-system/settings
- **Pages 设置**：https://github.com/wczlee9-bit/restaurant-system/settings/pages
- **Actions**：https://github.com/wczlee9-bit/restaurant-system/actions

## 截图参考

### 仓库主页面顶部导航

```
Code  |  Issues  |  Pull requests  |  Actions  |  Projects  |  Security  |  Insights  |  ⚙️ Settings
                                                                                   ↑
                                                                              点击这里
```

### Pages 设置页面

```
Build and deployment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Source: [GitHub Actions ▼]        ← 选择这个！

[ Save ]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 Deployed GitHub Pages site

Your site is live at:
https://wczlee9-bit.github.io/restaurant-system/

[ View deployment ]
```

## 需要帮助？

如果遇到问题，请：
1. 查看 Actions 运行日志
2. 查看浏览器控制台错误
3. 联系技术支持

---

**提示**：GitHub Pages 完全免费，无任何限制！部署成功后，你的前端将永久免费运行。

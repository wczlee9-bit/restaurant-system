# 解决前端"加载中"问题

## 问题现象
访问 `https://mellow-rabanadas-877f3e.netlify.app/customer_order_v3.html` 一直显示"加载中"，看不到菜品。

## 问题根源
**后端数据库为空**！Render 后端服务正常运行，但数据库中没有：
- 菜品数据
- 桌号数据
- 店铺数据

因此前端 API 调用返回空数组，导致页面一直加载。

---

## ✅ 解决方案：使用数据初始化页面

### 步骤 1：访问初始化页面

打开以下链接：
```
https://mellow-rabanadas-877f3e.netlify.app/init_data.html
```

### 步骤 2：检查后端状态

页面会自动检查后端状态，确认后端服务是否正常运行。

**预期结果**：
```
✅ 后端正常运行
```

### 步骤 3：初始化数据

点击 **"初始化数据"** 按钮。

**注意**：
- 首次初始化可能需要 30-60 秒
- 如果超时，请再次点击
- 等待显示 `✅ 数据初始化成功！`

### 步骤 4：验证数据

点击 **"验证数据"** 按钮，确认数据已正确创建。

**预期结果**：
```
桌号: 10 个
分类: 4 个
菜品: 18 道
```

### 步骤 5：测试点餐页面

打开点餐页面：
```
https://mellow-rabanadas-877f3e.netlify.app/customer_order_v3.html?table=1
```

**预期结果**：
- ✅ 能看到菜品列表
- ✅ 可以点餐
- ✅ 可以提交订单

---

## 🔍 数据初始化说明

### 初始化内容包括：

1. **公司信息**
   - 名称：测试餐饮公司
   - 联系人：管理员
   - 电话：13800138000

2. **店铺信息**
   - 名称：美味餐厅
   - 地址：北京市海淀区测试街88号
   - 营业时间：每天 9:00-22:00（周末延长至 23:00）

3. **菜品分类**（4个）
   - 热菜
   - 凉菜
   - 主食
   - 饮品

4. **菜品数据**（18道）
   - 宫保鸡丁、鱼香肉丝、红烧肉等热菜
   - 凉拌黄瓜、皮蛋豆腐等凉菜
   - 米饭、蛋炒饭、牛肉面等主食
   - 可乐、雪碧、橙汁等饮品

5. **桌号**（10张）
   - 1-10 号桌
   - 座位数：2-10 人

---

## 🛠️ 故障排除

### 问题 1：初始化超时

**原因**：Render 免费实例可能处于休眠状态，需要时间唤醒。

**解决方案**：
- 等待 30-60 秒
- 再次点击"初始化数据"
- 如果仍然超时，可以在浏览器中直接访问后端 API 唤醒：
  ```
  https://restaurant-system-vzj0.onrender.com/health
  ```

### 问题 2：初始化失败

**检查事项**：
1. 后端服务是否正常运行：访问 `/health` 端点
2. 数据库连接是否正常：查看后端日志
3. 网络连接是否正常：检查 Render 控制台

**解决方案**：
- 在 Render 控制台查看服务日志
- 检查数据库连接状态
- 必要时重启 Render 服务

### 问题 3：点餐页面仍显示"加载中"

**检查步骤**：
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 查看是否有错误信息
4. 切换到 Network 标签
5. 刷新页面，查看 API 请求是否返回数据

**常见错误**：
- `Failed to fetch`：后端服务未运行
- `404 Not Found`：API 端点不存在
- `500 Internal Server Error`：后端错误

---

## 📝 技术说明

### 为什么前端一直显示"加载中"？

前端代码逻辑：
```javascript
async function loadData() {
    // 1. 加载桌号
    const tables = await axios.get('/api/tables/');
    if (tables.data.length === 0) {
        // 没有桌号数据
        showWarning('未找到桌号数据，请先初始化数据');
        return;
    }

    // 2. 加载菜品分类
    const categories = await axios.get('/api/menu-categories/');
    // ...

    // 3. 加载菜品
    const menuItems = await axios.get('/api/menu-items/');
    // ...
}
```

如果数据库为空，API 返回空数组 `[]`，前端无法显示任何内容。

### 后端 API 代理配置

Netlify 将 `/api/*` 请求代理到 Render 后端：
```
/api/* -> https://restaurant-system-vzj0.onrender.com/api/:splat
```

因此前端调用 `/api/menu-items/` 实际上是访问：
```
https://restaurant-system-vzj0.onrender.com/api/menu-items/
```

---

## 🎯 完整测试流程

### 1. 初始化数据
访问：`https://mellow-rabanadas-877f3e.netlify.app/init_data.html`
→ 点击"初始化数据"
→ 等待成功

### 2. 验证数据
点击"验证数据"
→ 确认有 10 个桌号、4 个分类、18 道菜品

### 3. 测试点餐
访问：`https://mellow-rabanadas-877f3e.netlify.app/customer_order_v3.html?table=1`
→ 选择菜品
→ 点击"确认订单"
→ 提交订单

### 4. 测试支付
选择支付方式（"马上付款"或"餐后付款"）
→ 点击"确认支付"

---

## 📞 技术支持

如果遇到问题，请提供：
1. 浏览器控制台错误信息（F12）
2. Network 标签中的 API 请求/响应
3. 初始化页面的状态信息

---

最后更新：2026-01-12
